"use client";
import{useState}from"react";import{answerAssistant,type AssistantContext,type AssistantQuestion}from"@/features/personal-assistant/personal-assistant";
type Props={context:AssistantContext;onAdd?:(r:NonNullable<ReturnType<typeof answerAssistant>["action"]>)=>void};
const questions:[AssistantQuestion,string][]=[["next","Was soll ich als Nächstes upgraden?"],["town_hall","Lohnt sich das Rathaus-Upgrade jetzt?"],["heroes_or_defense","Helden oder Verteidigung zuerst?"],["delay","Warum verzögert sich mein Plan?"],["save_time","Wie kann ich Zeit sparen?"],["strategy","Welche Strategie passt zu mir?"],["biggest_gap","Wo liegt mein größter Rückstand?"]];
export function PersonalAssistant({context,onAdd}:Props){const[selected,setSelected]=useState<AssistantQuestion>("next");const result=answerAssistant(selected,context);return <section className="rounded-3xl border border-white/10 bg-white/5 p-6 md:p-8"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-amber-300">Datenbasierter Assistent</p><h2 className="mt-2 text-2xl font-bold">Frag deinen Plan</h2><p className="mt-2 text-sm text-slate-400">Antworten werden ausschließlich aus deinem Account und den Planerberechnungen erzeugt.</p></div><div className="mt-5 flex flex-wrap gap-2">{questions.map(([id,label])=><button key={id} onClick={()=>setSelected(id)} className={`rounded-xl px-3 py-2 text-sm ${selected===id?"bg-amber-400 font-bold text-slate-950":"bg-slate-900 text-slate-300"}`}>{label}</button>)}</div><div className="mt-6 rounded-2xl bg-slate-900 p-5"><h3 className="text-xl font-bold">{result.title}</h3><p className="mt-3 text-slate-200">{result.answer}</p><ul className="mt-4 space-y-2 text-sm text-slate-400">{result.evidence.map((line)=><li key={line}>• {line}</li>)}</ul>{result.action&&onAdd?<button onClick={()=>onAdd(result.action!)} className="mt-5 rounded-xl bg-emerald-400 px-4 py-2 text-sm font-bold text-slate-950">Empfohlenes Upgrade einplanen</button>:null}</div></section>}
